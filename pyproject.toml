[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "GNNgraph"
authors = [{name = "Crowdliness.ORG", email = "x@Crowdliness.ORG"}]
dynamic = ["version", "description"]
